<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.com.yuzhushui.schedule.job.biz.dao.JobInfoCopyDao">
	<resultMap type="cn.com.yuzhushui.schedule.job.biz.entity.JobInfoCopy" id="BaseResultMap">
        <result property="id" column="id" javaType="Integer"/>
        <result property="name" column="name" javaType="String"/>
        <result property="group" column="group" javaType="String"/>
        <result property="type" column="type" javaType="String"/>
        <result property="time" column="time" javaType="String"/>
        <result property="cron" column="cron" javaType="String"/>
        <result property="urls" column="urls" javaType="String"/>
        <result property="classPath" column="class_path" javaType="String"/>
        <result property="invokePolicy" column="invoke_policy" javaType="String"/>
        <result property="isActivity" column="is_activity" javaType="Integer"/>
        <result property="desc" column="desc" javaType="String"/>
        <result property="createTime" column="create_time" javaType="Date"/>
        <result property="modifyTime" column="modify_time" javaType="Date"/>
        <result property="param" column="param" javaType="String"/>
	</resultMap>
	
	<sql id="full-columns" >
        t.id,
        t.name,
        t.group,
        t.type,
        t.time,
        t.cron,
        t.urls,
        t.class_path,
        t.invoke_policy,
        t.is_activity,
        t.desc,
        t.create_time,
        t.modify_time,
        t.param
	</sql>
	
	<!-- 查询条件 -->
	<sql id="queryCriteria">
		 <if test="@OGNL@isNotEmpty(id)">
            AND t.id = #{id}
         </if>
		 <if test="@OGNL@isNotEmpty(name)">
            AND t.name = #{name}
         </if>
		 <if test="@OGNL@isNotEmpty(group)">
            AND t.group = #{group}
         </if>
		 <if test="@OGNL@isNotEmpty(type)">
            AND t.type = #{type}
         </if>
		 <if test="@OGNL@isNotEmpty(time)">
            AND t.time = #{time}
         </if>
		 <if test="@OGNL@isNotEmpty(cron)">
            AND t.cron = #{cron}
         </if>
		 <if test="@OGNL@isNotEmpty(urls)">
            AND t.urls = #{urls}
         </if>
		 <if test="@OGNL@isNotEmpty(classPath)">
            AND t.class_path = #{classPath}
         </if>
		 <if test="@OGNL@isNotEmpty(invokePolicy)">
            AND t.invoke_policy = #{invokePolicy}
         </if>
		 <if test="@OGNL@isNotEmpty(isActivity)">
            AND t.is_activity = #{isActivity}
         </if>
		 <if test="@OGNL@isNotEmpty(desc)">
            AND t.desc = #{desc}
         </if>
		 <if test="@OGNL@isNotEmpty(createTime)">
            AND t.create_time = #{createTime}
         </if>
		 <if test="@OGNL@isNotEmpty(modifyTime)">
            AND t.modify_time = #{modifyTime}
         </if>
		 <if test="@OGNL@isNotEmpty(param)">
            AND t.param = #{param}
         </if>
	</sql>

	<insert id="insert" parameterType="cn.com.yuzhushui.schedule.job.biz.entity.JobInfoCopy" >
		<![CDATA[
	        INSERT INTO job_info_copy (
	            name ,
	            group ,
	            type ,
	            time ,
	            cron ,
	            urls ,
	            class_path ,
	            invoke_policy ,
	            is_activity ,
	            desc ,
	            create_time ,
	            modify_time ,
	            param 
	        ) VALUES (
	        	#{name},
	        	#{group},
	        	#{type},
	        	#{time},
	        	#{cron},
	        	#{urls},
	        	#{classPath},
	        	#{invokePolicy},
	        	#{isActivity},
	        	#{desc},
	        	#{createTime},
	        	#{modifyTime},
	        	#{param}
	        )
	    ]]>
    	<selectKey resultType="java.lang.Integer" keyProperty="id">
			SELECT LAST_INSERT_ID() AS id
        </selectKey>
	</insert>
	
	<insert id="insertBatch" parameterType="java.util.List" >
		<![CDATA[
	        INSERT INTO job_info_copy (
	            name ,
	            group ,
	            type ,
	            time ,
	            cron ,
	            urls ,
	            class_path ,
	            invoke_policy ,
	            is_activity ,
	            desc ,
	            create_time ,
	            modify_time ,
	            param 
	        ) VALUES 
	        <foreach collection="list" item="item" index="index" separator=",">
	        (
	        	#{name},
	        	#{group},
	        	#{type},
	        	#{time},
	        	#{cron},
	        	#{urls},
	        	#{classPath},
	        	#{invokePolicy},
	        	#{isActivity},
	        	#{desc},
	        	#{createTime},
	        	#{modifyTime},
	        	#{param}
	        )
	        </foreach>
	    ]]>
	</insert>
	
	<update id="delete" parameterType="java.lang.Integer">
        UPDATE job_info_copy SET deleted=1 
        WHERE deleted=0 AND
        	id = #{value}
	</update>
	<update id="deleteBatch" parameterType="list" >
        UPDATE job_info_copy SET deleted=1 
        WHERE deleted=0 AND
        	id IN 
        <foreach collection="list" item = "id" open="(" separator="," close=")">
        	#{id}
		</foreach>
	</update>
	
	<update id="update" parameterType="cn.com.yuzhushui.schedule.job.biz.entity.JobInfoCopy">
        UPDATE job_info_copy 
        <set>
        	<if test="@OGNL@isNotEmpty(name)">
	            name = #{name},
	        </if>
        	<if test="@OGNL@isNotEmpty(group)">
	            group = #{group},
	        </if>
        	<if test="@OGNL@isNotEmpty(type)">
	            type = #{type},
	        </if>
        	<if test="@OGNL@isNotEmpty(time)">
	            time = #{time},
	        </if>
        	<if test="@OGNL@isNotEmpty(cron)">
	            cron = #{cron},
	        </if>
        	<if test="@OGNL@isNotEmpty(urls)">
	            urls = #{urls},
	        </if>
        	<if test="@OGNL@isNotEmpty(classPath)">
	            class_path = #{classPath},
	        </if>
        	<if test="@OGNL@isNotEmpty(invokePolicy)">
	            invoke_policy = #{invokePolicy},
	        </if>
        	<if test="@OGNL@isNotEmpty(isActivity)">
	            is_activity = #{isActivity},
	        </if>
        	<if test="@OGNL@isNotEmpty(desc)">
	            desc = #{desc},
	        </if>
        	<if test="@OGNL@isNotEmpty(createTime)">
	            create_time = #{createTime},
	        </if>
        	<if test="@OGNL@isNotEmpty(modifyTime)">
	            modify_time = #{modifyTime},
	        </if>
        	<if test="@OGNL@isNotEmpty(param)">
	            param = #{param},
	        </if>
        </set>
        WHERE deleted=0
        	AND id = #{id}
	</update>
	
	<select id="getById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
	   	SELECT <include refid="full-columns" />
       	<![CDATA[
       	FROM job_info_copy t
       	WHERE t.id = #{value}
		]]>
    </select>
	
	<select id="query" parameterType="map" resultMap="BaseResultMap">
		SELECT <include refid="full-columns" />
	    FROM job_info_copy t
		<where> <include refid="queryCriteria"/>  </where>
		<if test="@OGNL@isNotEmpty(orderColumn) ">
        ORDER BY t.${orderColumn} <if test="@OGNL@isNotEmpty(orderDirection) ">${orderDirection}</if>
        </if>
	</select>
	
	<select id="queryPage" parameterType="map" resultMap="BaseResultMap">
		SELECT 
        <include refid="full-columns" />
            FROM job_info_copy t
		<where> <include refid="queryCriteria"/>  </where>
		<if test="@OGNL@isNotEmpty(orderColumn) ">
        ORDER BY t.${orderColumn} <if test="@OGNL@isNotEmpty(orderDirection) ">${orderDirection}</if>
        </if>
        LIMIT #{start}, #{pageSize}
	</select>

	<select id="queryCount" parameterType="map" resultType="java.lang.Integer">
		select count(id) FROM job_info_copy 
        <where> <include refid="queryCriteria"/>  </where>
	</select>
</mapper>
